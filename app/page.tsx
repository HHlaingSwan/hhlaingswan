import React from "react";
import ClientWrapper from "@/components/ClientWrapper";

const page = () => {
	return (
		<main id="main-content">
			<ClientWrapper />
		</main>
	);
};

export default page;
